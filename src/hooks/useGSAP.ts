// Import the lib to trigger SSR-safe plugin registration as a side-effect.
import { gsap, ScrollTrigger } from "@/lib/gsap";
import { useGSAP } from "@gsap/react";

export { useGSAP, gsap, ScrollTrigger };
